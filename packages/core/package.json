{
  "name": "tpeg-core",
  "type": "module",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "test": "bun test src/**/*.spec.ts",
    "build": "bun build --target=node ./index.ts --outdir=./dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  },
  "description": "Core types and utilities for tpeg PEG parser",
  "license": "MIT"
}
