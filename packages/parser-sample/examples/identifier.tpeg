grammar Identifier {
  @version: "1.0"
  @description: "Basic identifier parsing grammar"
  @author: "TPEG Parser"

  identifier = letter (letter / digit / underscore)*
  letter = [a-zA-Z]
  digit = [0-9]
  underscore = "_"
}
